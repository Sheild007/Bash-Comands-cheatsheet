[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "my_awesome_package"
version = "0.1.0"
authors = [
  { name="Usman", email="usman.muneer720@gmail.com" },
]
description = "A sample package for the shell 101 exercises"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests",
]

[project.urls]
"Homepage" = "https://github.com/Sheild007/Bash-Comands-cheatsheet"
